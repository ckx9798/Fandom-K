import Header from '../../components/Header';

const ListPage = () => {
    return (
        <div>
            <p>
                list
                <Header />
            </p>
        </div>
    );
};

export default ListPage;
